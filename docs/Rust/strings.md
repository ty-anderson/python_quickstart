# Rust Strings

In Rust there are two main types of strings:

1. ``&str`` - string literals. A borrowed reference to a string slice. Read only.
2. ``String`` - owned text that can be modified. Goes on the heap.

